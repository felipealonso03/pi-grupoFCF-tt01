<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head') %>

<body>

    <% if (locals.user != undefined) { %>
     
        <%- include('./partials/headerLog') %>

        <% } else { %>
         
            <%- include('./partials/header') %>
            
    <% } %>

    <main>
        <div class="user-detail container">
            <section class="top main-content">
                <img class="profile" src="<%= datos.fotoPerfil %>" alt="foto mía">
                <div class="info-usuario">
                    <p><%= listaPosteo.email %></p> 
                    <a class="follow" href="/profile/edit"><i class="fa-solid fa-user-gear"></i></a>
                    <ul class="meta">
                        <li><%= listaPosteo.length %></li>
                        <li>200 seguidores</li>
                        <li>300 seguidos</li>
                    </ul>
                </div>
            </section>
            <section class="bottom">
                <article class="post">
                    <% for( let i = 0; i < listaPosteo.length; i++ ) { %>
                    
                    
                    <a href="/posteos/postDetail/<%= listaPosteo.email %>">
                        <img src="<%= listaPosteo.url %>" alt="">
                    </a>
                    <% } %>
                </article>
                
            </section>
        </div>
    </main>

    <%- include('./partials/footer') %>
    
</body>

</html>